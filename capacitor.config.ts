import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.imposter.who',
  appName: 'Imposter Who?',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
